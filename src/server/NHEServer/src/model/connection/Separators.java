package model.connection;

/**
 *
 * @author Anderson
 */
public class Separators
{
        public static String OBJECTS = Character.toString(Character.toChars(1)[0]);
		public static String DELIMITERS = Character.toString(Character.toChars(2)[0]);
		public static String FIELDS = Character.toString(Character.toChars(3)[0]);
        public static String COMMANDS = Character.toString(Character.toChars(4)[0]);
        public static String PARAMETERS_BEGIN = Character.toString(Character.toChars(5)[0]);
		public static String PARAMETERS_END = Character.toString(Character.toChars(6)[0]);
}
